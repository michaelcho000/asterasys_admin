import clsx from 'clsx'

const AsterasysProductBadge = ({ className = '' }) => {
  return <span className={clsx('badge bg-primary ms-2', className)}>Asterasys</span>
}

export default AsterasysProductBadge
